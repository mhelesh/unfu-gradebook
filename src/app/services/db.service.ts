import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class DbService {

  constructor() { }

  //doc$(path): Observable<any> {return}
  doc$(path) { return }

  updateAt(path, data) { }
}
